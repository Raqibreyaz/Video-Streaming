- path.relative(directoryPath,absolutePath), this allows to make extract the path starting after 'directoryPath' from absolutePath

- whole directory uploads requires manually traversing the directory recursively and upload one by one

- it is better practice to abstract the callback based logics like child process handling in a separate promised based function and try to make the code flow flat

- it is not sure that if readStream has finished writing to writeStream and writeStream data is immediately drained and now empty, so return only when writeStream also has ended, pipeline() handles this implicitly

- @/* type imports intellisense works correctly but resolving to a normal js import requires a separate build tool as 'tsc' will not do it for you

- "executing the ECS from lambda requires 2 permissions":
  - ecs::RunTask and iam::PassRole
  - ecs::RunTask is on the task-definition of ECS while iam::PassRole is on the IAM role of the ECS
  - ecs::RunTask is to execute ECS job and iam:PassRole is to pass the ecsTaskExecutionRole role to ECS

need-to-learn-explicitly:
- Dockerfile configuration
- child_processes module

AWS_CONTAINER_CREDENTIALS_RELATIVE_URI