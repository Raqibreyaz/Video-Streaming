import { S3Event } from "aws-lambda";

// create a pre-signed url for client to directly upload to s3
const uploadFile = (file)=>{
}

// spin up a new transcoding job on file upload
const createJob = (event: S3Event) => {};

export const lamda = () => {};